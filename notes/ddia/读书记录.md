# DDIA 读书记录

## 2026-05-06

- DDIA Chapter 8：继续读 transaction，从弱隔离级别一路读到 `Materializing Conflicts`。重点梳理了 read committed、snapshot isolation/MVCC、lost update 的防止方式（atomic write、explicit locking、automatic detection、conditional write/CAS），以及 write skew、phantom read 为什么会在 snapshot isolation 下破坏跨 row 的 invariant。

## 2026-05-05

- DDIA Chapter 8：读完 `Transaction 到底是什么？ {#sec_transactions_overview}` 这一节。重点梳理了 transaction 的 programming model、ACID/BASE 的基本含义，以及 transaction 在 atomicity、consistency、isolation、durability 上分别提供什么 guarantee。

## 2026-05-04

- DDIA Chapter 7：读完第七章 sharding。重点梳理了 partition key、key-range sharding、hash sharding、rebalancing、consistent hashing、skew / hot shard / hot key、request routing，以及 local/global secondary index 在 sharding 后带来的查询、写入和一致性复杂度。

## 2026-05-03

- DDIA Chapter 6：读完第六章 replication。重点梳理了 multi-leader replication、replication lag 的一致性保证、write conflict / conflict resolution、CRDT 与 OT、leaderless replication、quorum reads/writes、read repair、hinted handoff、anti-entropy，以及 happens-before / version vector 如何用于检测 concurrent writes。

## 2026-05-02

- DDIA Chapter 6：从 replication 开头读到 `multi-leader replication` 之前，也就是 `single-leader replication`、replication method、以及 replication lag 带来的读一致性问题这一段。

## 2026-05-01

- DDIA Chapter 5：从 `Avro` 开始，读完 Avro、schema 的优点、数据流的模式、流经数据库 / 服务 / workflow / message broker / actor framework 的数据流，以及 Chapter 5 余下部分。

## 2026-04-30

- Chapter 4：读了一部分存储相关内容，并围绕 B-tree、列式存储、以及可以用代码验证的知识点整理/补充了笔记。
- Chapter 5：从“编码与演化”开始，读到 Avro 小节之前，也就是已经读完 JSON/XML/CSV、JSON Schema、二进制编码、MessagePack、Protocol Buffers 这一段。
